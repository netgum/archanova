{
  "name": "@archanova/wallet-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Archanova Wallet SDK",
  "main": "./index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/archanova/wallet-sdk.git"
  },
  "scripts": {
    "clean": "lerna clean --yes && lerna run clean --stream",
    "bootstrap": "lerna bootstrap",
    "prebuild": "npm run bootstrap",
    "build": "lerna run --stream build",
    "compile": "lerna run compile --stream",
    "precompile:watch": "npm run compile",
    "compile:watch": "lerna run compile:watch --parallel",
    "test": "lerna run test --stream",
    "start:example:tutorial:5100": "cross-env PORT=5100 lerna run start --scope \"@examples/tutorial\"",
    "start:example:tutorial:5200": "cross-env PORT=5200 lerna run start --scope \"@examples/tutorial\""
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "lerna": "^3.8.5"
  }
}
