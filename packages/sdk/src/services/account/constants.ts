export enum AccountStates {
  Created = 'Created',
  Deployed = 'Deployed',
}

export enum AccountDeployModes {
  Secured = 'Secured',
  Unsecured = 'Unsecured',
}

export enum AccountDeviceStates {
  Created = 'Created',
  Deployed = 'Deployed',
}

export enum AccountDeviceTypes {
  Owner = 'Owner',
  Delegate = 'Delegate',
  Virtual = 'Virtual',
}

export enum AccountTransactionTypes {
  Incoming = 'Incoming',
  Outgoing = 'Outgoing',
}
